services:
  - type: web
    name: outreach-monitor
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: outreach-db
          property: connectionString

databases:
  - name: outreach-db
    plan: free
